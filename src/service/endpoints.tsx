import config from 'config';

let endpoints = {
	//SSO
	// a: config.service.a,
}

let API_BOOK = {
	// UPDATE_PROFILE: {
	// 	url: endpoints.a,
	// 	method: 'GET',
	// 	headers: {
	// 		Authorization: 'Bearer ' + localStorage.getItem('authToken')
	// 	}
	// },
}

export default API_BOOK


